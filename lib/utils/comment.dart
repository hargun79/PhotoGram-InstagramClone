class Comment {
  String username, userId, userProfilePicUrl, body;
  String postId;
  DateTime dateTime;

  Comment(
      {this.username,
      this.userId,
      this.userProfilePicUrl,
      this.body,
      this.postId,
      this.dateTime});
}
